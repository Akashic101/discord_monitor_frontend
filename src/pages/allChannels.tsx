import '../props/userCard.css';
import { ChannelCardCollection } from '../props/channelCardCollection';

function AllChannels() {
  return (
    <ChannelCardCollection></ChannelCardCollection>
    )
}

export default AllChannels;
